apiVersion: v1
kind: Service
metadata:
  name: esquire-service
  labels:
    app: esquire-service
spec:
  ports:
  - port: 80
    protocol: TCP
    name: http
  selector:
    app: esquire-service
  type: NodePort
