apiVersion: v1
kind: Service
metadata:
  name: esquire-service
spec:
  selector:
    app: esquire-deployment
  ports:
    - protocol: TCP
      port: 8072
      targetPort: 8072
      nodePort: 8072
  type: NodePort
